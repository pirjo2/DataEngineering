plugins {
    id 'java'
}

repositories {
    mavenCentral() // Standard Maven repository
    maven { url 'https://maven.pkg.github.com/duckdb/duckdb' } // DuckDB's GitHub repository
}

dependencies {
    implementation group: 'org.duckdb', name: 'duckdb_jdbc', version: '1.1.3'
}